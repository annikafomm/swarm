# Copy this file to backend/.env (gitignored) and fill in a real secret.
# Generate one with:
#   python -c "import secrets; print(secrets.token_hex(32))"
# Each deployment (your devcontainer, the prod server) should have its own
# value — never commit the real secret or reuse it across deployments.
SESSION_SECRET_KEY=
